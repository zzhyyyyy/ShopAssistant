### 可见性
对于写操作，确保每次写操作都是写在相应主内存处而不是Cache块上
对于读操作，确保每次读取数据都是从主内存中读取，保证每次读到的数据都是最新的

### 不可优化性
强制声明避免编译器进行优化
```
volatile int i ;
i = 1;
printf("%d",i);
如果没用volatile的话 编译器会直接优化为printf("%d",1);
```

### 顺序执行
变量间的执行顺序保证和代码的顺序一致

### 注：volatile自身不具备原子性，若要实现volatile的原子性操作要通过原子操作`atomic`或锁`mutex`来实现


